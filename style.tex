\usepackage{array}

\graphicspath{{./graphics/}}

% quote block
\usepackage[most]{tcolorbox}
\definecolor{block-gray}{gray}{0.85}
\newtcolorbox{myquote}{
    colback=block-gray,grow to right by=-2em,grow to left by=-2em,
    boxrule=0pt,boxsep=0pt,breakable
}

% stylize hyper links
\hypersetup{
    colorlinks=true,       % Enable colored links (no bounding box)
    linkcolor=blue,        % Set the color of internal links
    urlcolor=blue,         % Set the color of URL links
    citecolor=blue         % Set the color of citation links
}

% reverseclip
\tikzset{even odd clip/.code={\pgfseteorule},
    reverseclip/.style={overlay,insert path={(-16383.99999pt,-16383.99999pt) rectangle (16383.99999pt,16383.99999pt)}}}

% tight nodes
\tikzset{tight/.style={
            inner sep=0pt,
            outer sep=0pt,
        }}

% diagram
\tikzset{diagram/.style={
            node distance=0.4cm,
        }}

% spelling
\tikzset{spelling/.style={
            inner sep=0pt,
            outer sep=0pt,
            node distance=0.4cm,
            anchor=center,
            font=\sffamily\bfseries,
        }}

% verb
\tikzset{verb/.style={
            node distance=0.2cm,
            minimum width=0.6cm,
        }}

% table
\newcolumntype{M}[1]{>{\centering\arraybackslash}m{#1}}
\tikzset{
    shade/.style={
            pattern=north east lines,
            pattern color=black!70,
        }
}
\tikzset{
    matrixtable/.style={
            matrix of nodes,
            nodes={
                    anchor=center,
                    minimum width=2em,
                    minimum height=2em,
                    draw=black,
                },
            draw=black,
            inner sep=0,
            outer sep=0,
            node distance=0,
        },
    matrixtable/green/.style={
            nodes={
                    draw=green,
                },
            draw=green,
        },
    matrixtable/red/.style={
            nodes={
                    draw=red,
                },
            draw=red,
        }
}