\usepackage{xcolor}

\newif\ifdebug
% \debugtrue
\debugfalse

% Color scheme from: https://www.dieseskleinebuch.de/
\definecolor{green}{HTML}{6ca877}
\definecolor{purple}{HTML}{805695}
\definecolor{yellow}{HTML}{e89959}
\definecolor{red}{HTML}{e06262}
\definecolor{gray}{HTML}{c7c1bc}
\definecolor{darkgray}{HTML}{7c7874}

\def\shapelinewidth{0.08em}

% Square
\pgfkeys{
    /square/.is family, /square,
    default/.style = {color = {none}, thickness = {normal}, border = {false}, fill = {full}, pattern = {none}, scale = {1.0}},
    color/.store in = \squarecolor,
    thickness/.store in = \squarethickness,
    border/.store in = \squareborder,
    fill/.store in = \squarefill, % full, half, none, large, middle, small
    pattern/.store in = \squarepattern, % vert, diag
    scale/.store in = \squarescale,
}

\newcommand\dsq[1]{
    (-#1/2,-#1/2) rectangle (#1/2,#1/2);
}

% must add `%` to prevent \NewDocumentCommand from adding extra spaces.
\NewDocumentCommand{\square}{o}{%
    \pgfkeys{/square, default, #1}%
    \begin{tikzpicture}[baseline={([yshift=-0.5ex]current bounding box.center)}]
        \def\a{2em*\squarescale} %side length
        \def\l{\shapelinewidth*\squarescale} %line width

        \expandafter\ifstrequal\expandafter{\squarethickness}{medium}{
            \def\l{\shapelinewidth*1.5}
        }{}
        \expandafter\ifstrequal\expandafter{\squarethickness}{high}{
            \def\l{\shapelinewidth*3}
        }{}

        \expandafter\ifstrequal\expandafter{\squareborder}{true}{
            \def\bordercolor{black};
        }{
            \def\bordercolor{none};
        }

        % only fill when \squarefill is full or large
        \def\fillcolor{none}
        \expandafter\ifstrequal\expandafter{\squarefill}{full}{
            \def\fillcolor{\squarecolor}
        }{}
        \expandafter\ifstrequal\expandafter{\squarefill}{large}{
            \def\fillcolor{\squarecolor}
        }{}

        % draw the outter square
        \draw[fill=\fillcolor, draw=\bordercolor, line width=\l] \dsq{\a}

        % half fill
        \expandafter\ifstrequal\expandafter{\squarefill}{half}{
            \draw[fill=\squarecolor, draw=none] (-\a/2+\l/2,-\a/2+\l/2) rectangle (\a/2-\l/2,\l/2);
        }{}

        \def\ai{\a*0.8} % inner square side length
        % inner square if specified
        \expandafter\ifstrequal\expandafter{\squarefill}{large}{
            \draw[fill=none, draw=black, line width=\l] \dsq{\ai}
        }{}
        \expandafter\ifstrequal\expandafter{\squarefill}{small}{
            \draw[fill=none, draw=black, line width=\l] \dsq{\ai}
        }{}

        \expandafter\ifstrequal\expandafter{\squarefill}{middle}{
            \draw[fill=\squarecolor, draw=black, line width=\l] \dsq{\ai}
        }{}

        % patterns
        \expandafter\ifstrequal\expandafter{\squarepattern}{vert}{
            \draw[draw=black, line width=\l, shift={(-\a/2, -\a/2)}]
            (\a/3, 0) -- (\a/3, \a)
            (\a/3*2, 0) -- (\a/3*2, \a);
        }{}
        \expandafter\ifstrequal\expandafter{\squarepattern}{diag}{
            \draw[draw=black, line width=\l, shift={(-\a/2, -\a/2)}]
            (0, \a/2) -- (\a/2, 0)
            (0, \a) -- (\a, 0)
            (\a/2, \a) -- (\a, \a/2);
        }{}

        \useasboundingbox \dsq{\a}

        \ifdebug
            \draw[draw=red, line width=\l] (-\a/2,0) -- (\a/2,0);
            \draw[draw=red, line width=\l] (0,-\a/2) -- (0,\a/2);
            \draw[draw=blue, dotted, line width=0.1em] (current bounding box.south west) rectangle(current bounding box.north east);
        \fi

    \end{tikzpicture}%
}

% Diamond
\pgfkeys{
    /diam/.is family, /diam,
    default/.style = {color = {none}, border = {false}, fill = {full}, scale={1.0}},
    color/.store in = \diamcolor,
    border/.store in = \diamborder,
    fill/.store in = \diamfill, % large, middle, small
    scale/.store in = \diamscale,
}

\newcommand\ddm[2]{
    (-#1/2,0) -- (0,#2/2) -- (#1/2,0) -- (0,-#2/2) -- cycle;
}

\NewDocumentCommand{\diam}{o}{%
    \pgfkeys{/diam, default, #1}%
    \begin{tikzpicture}[baseline={([yshift=-0.5ex]current bounding box.center)}]
        \def\a{2.5em*\diamscale}
        \def\b{2em*\diamscale}
        \def\l{\shapelinewidth*\diamscale} %line width

        \expandafter\ifstrequal\expandafter{\diamborder}{true}{
            \def\bordercolor{black};
        }{
            \def\bordercolor{none};
        }

        % only fill when \diamfill is full or large
        \def\fillcolor{none}
        \expandafter\ifstrequal\expandafter{\diamfill}{full}{
            \def\fillcolor{\diamcolor}
        }{}
        \expandafter\ifstrequal\expandafter{\diamfill}{large}{
            \def\fillcolor{\diamcolor}
        }{}

        \draw[fill=\fillcolor, draw=\bordercolor, line width=\l] \ddm{\a}{\b}

        % inner diamond
        \def\ai{\a*0.75}
        \def\bi{\b*0.75}
        \expandafter\ifstrequal\expandafter{\diamfill}{large}{
            \draw[fill=none, draw=\bordercolor, line width=\l] \ddm{\ai}{\bi}
        }{}
        \expandafter\ifstrequal\expandafter{\diamfill}{small}{
            \draw[fill=none, draw=\bordercolor, line width=\l] \ddm{\ai}{\bi}
        }{}
        \expandafter\ifstrequal\expandafter{\diamfill}{middle}{
            \draw[fill=\diamcolor, draw=\bordercolor, line width=\l] \ddm{\ai}{\bi}
        }{}

        \ifdebug
            \draw[draw=red, line width=\l] (-\a/2,0) -- (\a/2,0);
            \draw[draw=red, line width=\l] (0,-\b/2) -- (0,\b/2);
        \fi

    \end{tikzpicture}%
}

% Olive
\pgfkeys{
    /olive/.is family, /olive,
    default/.style = {color = {none}, border = {false}, fill = {full}},
    color/.store in = \olivecolor,
    border/.store in = \oliveborder,
    fill/.store in = \olivefill, % large, middle, small
    scale/.store in = \olivescale,
}

\newcommand\dol[2]{
let
\n1 = {(#1*#1+#2*#2)/4/(#2)},
\n2 = {atan2(2*\n1-#2, (#1))} % note that the parameters must be enclosed with parethesis or else \a*0.8 won't work
in
(#1/2,0) arc[start angle=\n2, end angle=180-\n2, x radius=\n1, y radius=\n1]
(#1/2,0) arc[start angle=-\n2, end angle=-180+\n2, x radius=\n1, y radius=\n1];
}

\NewDocumentCommand{\olive}{o}{%
    \pgfkeys{/olive, default, #1}%
    \begin{tikzpicture}[baseline={([yshift=-0.5ex]current bounding box.center)}]
        \def\a{2em}
        \def\b{1.4em}
        \def\l{\shapelinewidth}

        \expandafter\ifstrequal\expandafter{\oliveborder}{true}{
            \def\bordercolor{black};
        }{
            \def\bordercolor{none};
        }

        % only fill when \olivefill is full or large
        \def\fillcolor{none}
        \expandafter\ifstrequal\expandafter{\olivefill}{full}{
            \def\fillcolor{\olivecolor}
        }{}
        \expandafter\ifstrequal\expandafter{\olivefill}{large}{
            \def\fillcolor{\olivecolor}
        }{}

        \draw[fill=\fillcolor, draw=\bordercolor, line width=\l] \dol{\a}{\b}

        % inner olive
        \def\ai{\a*0.8}
        \def\bi{\b*0.8}
        \expandafter\ifstrequal\expandafter{\olivefill}{large}{
            \draw[fill=none, draw=\bordercolor, line width=\l] \dol{\ai}{\bi}
        }{}
        \expandafter\ifstrequal\expandafter{\olivefill}{small}{
            \draw[fill=none, draw=\bordercolor, line width=\l] \dol{\ai}{\bi}
        }{}
        \expandafter\ifstrequal\expandafter{\olivefill}{middle}{
            \draw[fill=\olivecolor, draw=\bordercolor, line width=\l] \dol{\ai}{\bi}
        }{}

        \ifdebug
            \draw[draw=red, line width=\l] (-\a/2,0) -- (\a/2, 0);
            \draw[draw=red, line width=\l] (0,-\b/2) -- (0,\b/2);
        \fi

    \end{tikzpicture}%
}

% Triangle (left pointing)
\pgfkeys{
    /tri/.is family, /tri,
    default/.style = {color = {none}, border = {false}, fill = {full}},
    color/.store in = \tricolor,
    border/.store in = \triborder,
    fill/.store in = \trifill, % large, middle, small
    scale/.store in = \triscale,
}

\newcommand\dtr[2]{
    (-#1/2, 0) -- (#1/2, #2/2) -- (#1/2, -#2/2) -- cycle;
}

\NewDocumentCommand{\tri}{o}{%
    \pgfkeys{/tri, default, #1}%
    \begin{tikzpicture}[baseline={([yshift=-0.5ex]current bounding box.center)}]
        \def\a{2em}
        \def\b{1.5em}
        \def\l{\shapelinewidth}

        \expandafter\ifstrequal\expandafter{\triborder}{true}{
            \def\bordercolor{black};
        }{
            \def\bordercolor{none};
        }

        % only fill when \trifill is full or large
        \def\fillcolor{none}
        \expandafter\ifstrequal\expandafter{\trifill}{full}{
            \def\fillcolor{\tricolor}
        }{}
        \expandafter\ifstrequal\expandafter{\trifill}{large}{
            \def\fillcolor{\tricolor}
        }{}

        \draw[fill=\fillcolor, draw=\bordercolor, line width=\l] \dtr{\a}{\b}

        % inner triangle
        \def\ai{\a*0.7}
        \def\bi{\b*0.7}

        \expandafter\ifstrequal\expandafter{\trifill}{large}{
            \draw[fill=none, draw=\bordercolor, line width=\l, shift={(\a*0.07, 0)}] \dtr{\ai}{\bi}
        }{}
        \expandafter\ifstrequal\expandafter{\trifill}{small}{
            \draw[fill=none, draw=\bordercolor, line width=\l, shift={(\a*0.07, 0)}] \dtr{\ai}{\bi}
        }{}
        \expandafter\ifstrequal\expandafter{\trifill}{middle}{
            \draw[fill=\tricolor, draw=\bordercolor, line width=\l, shift={(\a*0.07, 0)}] \dtr{\ai}{\bi}
        }{}

        \ifdebug
            \draw[draw=red, line width=\l] (-\a/2,0) -- (\a/2, 0);
            \draw[draw=red, line width=\l] (0,-\b/2) -- (0,\b/2);
        \fi
    \end{tikzpicture}%
}

% Pill
\pgfkeys{
    /pill/.is family, /pill,
    default/.style = {color = {none}, border = {false}, width={1.3em}, radius={0.6em}, shape={type=none, size=small, color=none}, label = {}},
    color/.store in = \pillcolor,
    border/.store in = \pillborder,
    label/.store in = \pilllabel,
    width/.store in = \pillwidth,
    radius/.store in = \pillradius,
    shape/.code={
            \pgfkeys{/pill/shape/.cd, #1}
        },
    shape/type/.store in=\pillshapetype, % diamond, olive, triangle, neutral
    shape/size/.store in=\pillshapesize,
    shape/color/.store in=\pillshapecolor,
}

\newcommand\dpi[2]{
    (-#1/2, -#2) -- (#1/2, -#2) arc[start angle=-90, end angle=90, radius=#2] --
    (#1/2, #2) -- (-#1/2, #2) arc[start angle=90, end angle=270, radius=#2] -- cycle;
}

\NewDocumentCommand{\pill}{o}{%
    \pgfkeys{/pill, default, #1}%
    \begin{tikzpicture}[baseline={([yshift=-0.5ex]current bounding box.center)}]
        \def\a{\pillwidth}
        \def\r{\pillradius}
        \def\l{\shapelinewidth}

        \expandafter\ifstrequal\expandafter{\pillborder}{true}{
            \def\bordercolor{black};
        }{
            \def\bordercolor{none};
        }

        \begin{scope}
            \expandafter\ifstrequal\expandafter{\pillshapesize}{big}{
                \expandafter\ifstrequal\expandafter{\pillshapetype}{diamond}{
                    \clip[overlay,even odd clip,reverseclip, shift={(\a/2+\r*0.4,0)}] \ddm{\r*2.1}{\r*2.1};
                }{}
                \expandafter\ifstrequal\expandafter{\pillshapetype}{olive}{
                    \clip[overlay,even odd clip,reverseclip, shift={(\a/2+\r*0.3,0)}] \dol{\r*2.1}{\r*1.5}
                }{}
                \expandafter\ifstrequal\expandafter{\pillshapetype}{triangle}{
                    \clip[overlay,even odd clip,reverseclip, shift={(\a/2-\r*0.1,0)}] \dtr{\r*2.1}{\r*1.8}
                }{}
            }{}
            \draw[fill=\pillcolor, draw=\bordercolor, line width=\l] \dpi{\a}{\r}
        \end{scope}

        \node[align=center, anchor=center] at (0, 0) {\pilllabel};

        % extra shapes
        \expandafter\ifstrequal\expandafter{\pillshapesize}{small}{
            \expandafter\ifstrequal\expandafter{\pillshapetype}{diamond}{
                \draw[fill=\pillshapecolor, draw=black, line width=\l, shift={(\a/2-\r*0.1,0)}] \ddm{\r*1.5}{\r*1.5};
            }{}
            \expandafter\ifstrequal\expandafter{\pillshapetype}{olive}{
                \draw[fill=\pillshapecolor, draw=black, line width=\l, shift={(\a/2-\r*0.1,0)}] \dol{\r*1.7}{\r*1.2}
            }{}
            \expandafter\ifstrequal\expandafter{\pillshapetype}{triangle}{
                \draw[fill=\pillshapecolor, draw=black, line width=\l, shift={(\a/2-\r*0.1,0)}] \dtr{\r*1.5}{\r*1.5}
            }{}
        }{
            \expandafter\ifstrequal\expandafter{\pillshapetype}{diamond}{
                \draw[fill=\pillshapecolor, draw=black, line width=\l, shift={(\a/2+\r*0.4,0)}] \ddm{\r*2.1}{\r*2.1};
            }{}
            \expandafter\ifstrequal\expandafter{\pillshapetype}{olive}{
                \draw[fill=\pillshapecolor, draw=black, line width=\l, shift={(\a/2+\r*0.3,0)}] \dol{\r*2.1}{\r*1.5}
            }{}
            \expandafter\ifstrequal\expandafter{\pillshapetype}{triangle}{
                \draw[fill=\pillshapecolor, draw=black, line width=\l, shift={(\a/2-\r*0.1,0)}] \dtr{\r*2.1}{\r*1.8}
            }{}
        }

        \expandafter\ifstrequal\expandafter{\pillshapetype}{neutral}{
            \pgfmathsetmacro\sb{sin(60)}; \pgfmathsetmacro\cb{cos(60)}
            \def\t{\r*0.7}
            \draw[draw=black, line width=\l, shift={(-\r*0.4, 0)}]
            (-\a/2, 0) -- (-\a/2+\t*\cb, \t*\sb)
            (-\a/2, 0) -- (-\a/2+\t*\cb, -\t*\sb);
            \draw[draw=black, line width=\l, shift={(\r*0.4, 0)}]
            (\a/2, 0) -- (\a/2-\t*\cb, \t*\sb)
            (\a/2, 0) -- (\a/2-\t*\cb, -\t*\sb);
        }{}

        \useasboundingbox \dpi{\a}{\r}

        \ifdebug
            \draw[draw=red, line width=\l] (-\a/2-\r,0) -- (\a/2+\r, 0);
            \draw[draw=red, line width=\l] (0,-\r) -- (0,\r);
        \fi
    \end{tikzpicture}%
}

% Circle
\pgfkeys{
    /cir/.is family, /cir,
    default/.style = {color = {white}, thickness = {normal}, border = {false}, pattern = {none}, side = {false}, clock = {none}, rotate = {false}, scale = {1.0}},
    color/.store in = \circolor,
    border/.store in = \cirborder,
    thickness/.store in = \cirthickness,
    pattern/.store in = \cirpattern, % vert
    side/.store in = \cirside,
    clock/.store in = \circlock, % none, simple, hollow, double, dot
    rotate/.store in = \cirrotate,
    scale/.store in = \cirscale,
}

% draw "sidepill"
\newcommand\dsp[3]{
% 1. a, 2. r, 3. R
let
\n1 = {atan2(#3, (#2/2))} % note that the parameters must be enclosed with parethesis or else \a*0.8 won't work
in
(0, -#2) -- (-#1, -#2)
arc[start angle=270, end angle=90, x radius=#2, y radius=#2]
(-#1, #2) -- (0, #2)
arc[start angle=90, end angle=270, x radius=#3, y radius=#2];
}

\NewDocumentCommand{\cir}{o}{%
    \pgfkeys{/cir, default, #1}%
    \begin{tikzpicture}[baseline={([yshift=-0.5ex]current bounding box.center)}]
        \def\r{1em*\cirscale}
        \def\l{\shapelinewidth}
        \expandafter\ifstrequal\expandafter{\cirthickness}{medium}{
            \def\l{\shapelinewidth*1.5}
        }{}
        \expandafter\ifstrequal\expandafter{\cirthickness}{high}{
            \def\l{\shapelinewidth*3}
        }{}

        \expandafter\ifstrequal\expandafter{\cirborder}{true}{
            \def\bordercolor{black};
        }{
            \def\bordercolor{none};
        }

        \expandafter\ifstrequal\expandafter{\cirrotate}{true}{
            \def\grouprotate{45}
        }{
            \def\grouprotate{0}
        }

        \begin{scope}[rotate=\grouprotate]
            \draw[fill=\circolor, draw=\bordercolor, line width=\l](0,0) circle (\r);

            % patterns
            \pgfmathsetmacro\sa{sin(75)}; \pgfmathsetmacro\ca{cos(75)}
            \pgfmathsetmacro\sb{sin(60)}; \pgfmathsetmacro\cb{cos(60)}
            \pgfmathsetmacro\sc{sin(45)}; \pgfmathsetmacro\cc{sin(45)}
            \pgfmathsetmacro\sd{sin(15)}; \pgfmathsetmacro\cd{cos(15)}
            \expandafter\ifstrequal\expandafter{\cirpattern}{vert}{
                \draw[draw=black, line width=\l]
                (-\r/2, -\r*\sb) -- (-\r/2, \r*\sb)
                (0, -\r) -- (0, \r)
                (\r/2, -\r*\sb) -- (\r/2, \r*\sb);
            }{}

            % clock
            \def\w{0.25*\r}
            \def\h{0.8*\r}
            \expandafter\ifstrequal\expandafter{\circlock}{simple}{
                \draw[draw=black, line width=\l, fill=black, shift={(0, \r*0.3)}] (-\w/2, 0) rectangle (\w/2, \h);
            }{}
            \expandafter\ifstrequal\expandafter{\circlock}{hollow}{
                \draw[draw=black, line width=\l, fill=\circolor, shift={(0, \r*0.3)}] (-\w/2, 0) rectangle (\w/2, \h);
            }{}
            \expandafter\ifstrequal\expandafter{\circlock}{double}{
                \draw[draw=black, line width=\l, fill=\circolor, shift={(-\w*0.85, \r*0.3)}] (-\w/2, 0) rectangle (\w/2, \h);
                \draw[draw=black, line width=\l, fill=\circolor, shift={(\w*0.85, \r*0.3)}] (-\w/2, 0) rectangle (\w/2, \h);
            }{}
            \expandafter\ifstrequal\expandafter{\circlock}{dot}{
                \draw[draw=black, line width=\l, fill=black, shift={(0, \r*0.3)}] (-\w/2, 0) rectangle (\w/2, \h);
                \draw[draw=black, line width=\l, fill=black] (0, 0) circle (\w/2);
            }{}

        \end{scope}

        % side
        \expandafter\ifstrequal\expandafter{\cirside}{true}{
            \draw[fill=\circolor, draw=\bordercolor, line width=\l, shift={(-\r, 0)}] \dsp{\r*0.5}{\r*0.5}{\r*0.2}
        }{}

        \ifdebug
            \draw[draw=red, line width=\l] (-\r,0) -- (\r, 0);
            \draw[draw=red, line width=\l] (0,-\r) -- (0,\r);
        \fi
    \end{tikzpicture}%
}

% Flower
\pgfkeys{
    /flower/.is family, /flower,
    default/.style = {color = {white}, pattern = {none}, side = {false}, fill = {full}, clock = {none}, rotate = {false}, scale = {1.0}},
    color/.store in = \flowercolor,
    pattern/.store in = \flowerpattern, % vert
    side/.store in = \flowerside,
    fill/.store in = \flowerfill, % full, thin, thick
    clock/.store in = \flowerclock, % none, simple, hollow, double, dot
    rotate/.store in = \flowerrotate,
    scale/.store in = \flowerscale,
}

\newcommand\dfl[2]{
([shift={(0:#1)}]0,0)
foreach \angle in {0,60,...,300} {
let
\p1 = ([shift={(\angle:#1)}]0,0), %A
\p2 = ([shift={({\angle+90}:0.28*#1)}]\p1), %A1, control point
\p3 = ([shift={({\angle+30}:{#1-#2})}]0,0), %B
\p4 = ([shift={({\angle+30-90}:{0.12*#1})}]\p3), %B1, control point
\p5 = ([shift={({\angle+30+90}:{0.12*#1})}]\p3), %B2, control point
\p6 = ([shift={({\angle+60}:{#1})}]0,0), %C
\p7 = ([shift={({\angle-30}:{0.28*#1})}]\p6) %C1
in
-- (\p1) .. controls (\p2) and (\p4) ..
(\p3) .. controls (\p5) and (\p7) .. (\p6)
} -- cycle;
}

\NewDocumentCommand{\flower}{o}{%
    \pgfkeys{/flower, default, #1}%
    \begin{tikzpicture}[baseline={([yshift=-0.5ex]current bounding box.center)}]
        \def\r{1em}
        \def\b{0.2em}
        \def\l{\shapelinewidth}

        \expandafter\ifstrequal\expandafter{\flowerrotate}{true}{
            \def\grouprotate{45}
        }{
            \def\grouprotate{0}
        }

        \begin{scope}[rotate=\grouprotate]
            \begin{scope}
                \expandafter\ifstrequal\expandafter{\flowerfill}{thick}{
                    \clip[overlay, even odd clip,reverseclip]
                    \dfl{\r*0.55}{\b*0.55};
                }{}
                \expandafter\ifstrequal\expandafter{\flowerfill}{thin}{
                    \clip[overlay, even odd clip,reverseclip]
                    \dfl{\r*0.7}{\b*0.7};
                }{}
                \path[fill=\flowercolor, draw=none] \dfl{\r}{\b}
            \end{scope}

            % pattern
            \pgfmathsetmacro\sb{sin(60)}; \pgfmathsetmacro\cb{cos(60)}
            \expandafter\ifstrequal\expandafter{\flowerpattern}{vert}{
                \begin{scope}
                    \clip \dfl{\r}{\b}; % use mask so that the line won't exceed the shape
                    \draw[draw=black, line width=\l]
                    (-\r/2, -\r*\sb) -- (-\r/2, \r*\sb)
                    (0, -\r+\b) -- (0, \r-\b)
                    (\r/2, -\r*\sb) -- (\r/2, \r*\sb);
                \end{scope}
            }{}

            % clock
            \def\w{0.25*\r}
            \def\h{0.8*\r}
            \expandafter\ifstrequal\expandafter{\flowerclock}{simple}{
                \draw[draw=black, line width=\l, fill=black, shift={(0, \r*0.3)}] (-\w/2, 0) rectangle (\w/2, \h);
            }{}
            \expandafter\ifstrequal\expandafter{\flowerclock}{hollow}{
                \draw[draw=black, line width=\l, fill=\flowercolor, shift={(0, \r*0.3)}] (-\w/2, 0) rectangle (\w/2, \h);
            }{}
            \expandafter\ifstrequal\expandafter{\flowerclock}{double}{
                \draw[draw=black, line width=\l, fill=\flowercolor, shift={(-\w*0.85, \r*0.3)}] (-\w/2, 0) rectangle (\w/2, \h);
                \draw[draw=black, line width=\l, fill=\flowercolor, shift={(\w*0.85, \r*0.3)}] (-\w/2, 0) rectangle (\w/2, \h);
            }{}
            \expandafter\ifstrequal\expandafter{\flowerclock}{dot}{
                \draw[draw=black, line width=\l, fill=black, shift={(0, \r*0.3)}] (-\w/2, 0) rectangle (\w/2, \h);
                \draw[draw=black, line width=\l, fill=black] (0, 0) circle (\w/2);
            }{}
        \end{scope}

        % side
        \expandafter\ifstrequal\expandafter{\flowerside}{true}{
            \draw[fill=\flowercolor, draw=none, shift={(-\r*0.8, 0)}] \dsp{\r*0.8}{\r*0.5}{\r*0.4}
        }{}

        \useasboundingbox \dfl{\r}{\b}

        \ifdebug
            \draw[draw=red, line width=\l] (-\r,0) -- (\r,0);
            \draw[draw=red, line width=\l] (0,-\r) -- (0,\r);
            \draw[draw=blue, dotted, line width=0.1em] (current bounding box.south west) rectangle(current bounding box.north east);
        \fi
    \end{tikzpicture}%
}

\pgfkeys{
    /hook/.is family, /hook,
    default/.style = {type={middle}, scale={1}},
    type/.store in = \hooktype, % middle, left, right, circle
    scale/.store in = \hookscale,
}

\NewDocumentCommand{\hook}{o}{%
    \pgfkeys{/hook, default, #1}%
    \begin{tikzpicture}[baseline={([yshift=-0.5ex]current bounding box.center)}]
        \def\a{3em}
        \def\l{\shapelinewidth}

        \expandafter\ifstrequal\expandafter{\hooktype}{middle}{
            \draw[draw=black, line width=\l]
            (-\a/2, 0) -- (-\a/6, 0)
            arc[start angle=-180, end angle=0, x radius=\a/6, y radius=\a/6]
            (\a/6, 0) -- (\a/2, 0);
        }

        \expandafter\ifstrequal\expandafter{\hooktype}{left}{
            \draw[draw=black, line width=\l]
            (-\a/2, 0) arc[start angle=-180, end angle=0, x radius=\a/6, y radius=\a/6]
            (-\a/6, 0) -- (\a/2, 0);
        }

        \expandafter\ifstrequal\expandafter{\hooktype}{right}{
            \draw[draw=black, line width=\l]
            (-\a/2, 0) -- (\a/6, 0)
            arc[start angle=-180, end angle=0, x radius=\a/6, y radius=\a/6];
        }

        \ifdebug
            \draw[draw=red, line width=\l] (-\a/2,0) -- (\a/2,0);
            \draw[draw=red, line width=\l] (0,-\a/6) -- (0,\a/6);
        \fi
    \end{tikzpicture}%
}

% Sidepill
\pgfkeys{
    /sidepill/.is family, /sidepill,
    default/.style = {color = {white}, border = {false}, scale = {1.0}},
    color/.store in = \sidepillcolor,
    border/.store in = \sidepillborder,
    scale/.store in = \sidepillscale,
}

\NewDocumentCommand{\sidepill}{o}{%
    \pgfkeys{/sidepill, default, #1}%
    \begin{tikzpicture}[baseline={([yshift=-0.5ex]current bounding box.center)}]
        \def\r{1em*\sidepillscale}
        \def\l{\shapelinewidth}

        \expandafter\ifstrequal\expandafter{\sidepillborder}{true}{
            \def\bordercolor{black};
        }{
            \def\bordercolor{none};
        }

        \draw[fill=\sidepillcolor, draw=\bordercolor] \dsp{\r*0.5}{\r*0.5}{\r*0.2}

        \ifdebug
            \draw[draw=red, line width=\l] (-\r,0) -- (\r,0);
            \draw[draw=red, line width=\l] (0,-\r) -- (0,\r);
        \fi
    \end{tikzpicture}%
}

% Rectangle
\pgfkeys{
    /rect/.is family, /rect,
    default/.style = {color = {white}, border = {false}, fill = {full}, label={}, width={1em}, height={1em}},
    color/.store in = \rectcolor,
    border/.store in = \rectborder,
    fill/.store in = \rectfill,
    label/.store in = \rectlabel,
    width/.store in = \rectwidth,
    height/.store in = \rectheight,
}

\NewDocumentCommand{\rect}{o}{%
    \pgfkeys{/rect, default, #1}%
    \begin{tikzpicture}[baseline={([yshift=-0.5ex]current bounding box.center)}]
        \def\a{2em}
        \def\l{\shapelinewidth}

        \expandafter\ifstrequal\expandafter{\rectborder}{true}{
            \def\bordercolor{black};
        }{
            \def\bordercolor{none};
        }
        \draw[fill=\rectcolor, draw=\bordercolor, line width=\l] (-\rectwidth/2,-\rectheight/2) rectangle (\rectwidth/2,\rectheight/2);

        \node[align=center, anchor=center] at (0,0) {\rectlabel};

        \ifdebug
            \draw[draw=red, line width=\l] (-\rectwidth/2,0) -- (\rectwidth/2, 0);
            \draw[draw=red, line width=\l] (0,-\rectheight/2) -- (0,\rectheight/2);
        \fi
    \end{tikzpicture}%
}


% Card
\pgfkeys{
    /card/.is family, /card,
    default/.style = {width = {17em}, height = {10em}},
    width/.store in = \cardwidth,
    height/.store in = \cardheight,
    label/.store in = \cardlabel,
}
\NewDocumentCommand{\card}{o m}{%
    \pgfkeys{/card, default, #1}%
    \begin{tikzpicture}
        \def\barh{4ex}
        \def\r{0.5em}

        % background
        \draw[rounded corners=0.5em, draw=black, line width=0.1em ]
        (0, 0) rectangle(\cardwidth, \cardheight);

        % top bar
        \draw[thick, fill=blue!10]
        (0, \cardheight-\barh) -- (0, \cardheight-\r) arc[start angle=180, end angle=90, radius=\r] --
        (\r, \cardheight) -- (\cardwidth-\r, \cardheight) arc[start angle=90, end angle=0, radius=\r] --
        (\cardwidth, \cardheight-\r) -- (\cardwidth, \cardheight-\barh) -- cycle;
        \node[anchor=west, font=\bfseries, xshift=0.5em, yshift=-0.2em] at (0, \cardheight-\barh/2) {\cardlabel};

        \node[anchor=north, text width=\cardwidth-1em] at (\cardwidth/2, \cardheight-\barh) {#2};
    \end{tikzpicture}%
}

% Clock
\pgfkeys{
    /clock/.is family, /clock,
    default/.style = {hand = {false}},
    hand/.store in = \clockhand,
}
\NewDocumentCommand{\clock}{o}{%
    \pgfkeys{/clock, default, #1}%
    \begin{tikzpicture}
        % Draw clock face
        \draw[thick] (0,0) circle (9em);

        % Draw clock ticks
        \foreach \angle in {0,90,...,360} {
                \draw[thick] (\angle:8em) -- (\angle:9em);
            }

        % Add 4 ticks
        \node[font=\large] at (90:10em) {\#};
        \node[font=\large] at (270:10em) {halb \#+1};
        \node[font=\large] at (0:11.5em) {vier.};
        \node[font=\large] at (180:11.5em) {vier.};

        % Add clockwise direction arrow
        \draw[->, thick]
        (150:11em)
        arc (150:120:11em)
        node[midway, left] {vor};

        % Add counterclockwise direction arrow
        \draw[->, thick]
        (30:11em)
        arc (30:60:11em)
        node[midway, right] {nach};
    \end{tikzpicture}%
}